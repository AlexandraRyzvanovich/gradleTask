plugins {
    id 'java'
}

group 'com.epam'
version '1.8'

repositories {
    mavenCentral()
    flatDir (
            dirs: 'lib'
    )
}

dependencies {
    testImplementation group: 'junit', name: 'junit', version: '4.12'
    implementation fileTree(include: ['*.jar'], dir: 'libs')
}
